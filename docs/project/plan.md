# План инициализации проекта: 1C Far Plugin

## Обзор

Документ описывает план подготовки и реализации мультиплатформенного плагина для FAR Manager / far2l, обеспечивающего прозрачную работу с бинарными артефактами платформы 1С:Предприятие 8.

**Язык реализации:** Rust с C-совместимым интерфейсом (C ABI) для Far Plugin API

**Целевые платформы:** Windows (FAR Manager 3), Linux (far2l)

**Основные источники:**

- docs/cf_processing.md — внутренняя спецификация алгоритмов парсинга CF
- tool1cd (GitHub e8tools) — формат .1cd
- v8asm (GitHub EvilBeaver) — справочник по байт-коду

---

## Итоговая картина состояния

```text
Фаза 0 (Подготовка окружения)  ██████████  100% завершена
Фаза 1 (MVP — Базовый каркас)  ██████████  100% завершена
Фаза 2 (EPF/ERF — VFS дерево)  ████████░░  80% (2.2-2.8 выполнены)
Фаза 3–6                       ░░░░░░░░░░  не начаты
```

## Статус проекта (Фаза 0: Подготовка окружения)

| # | Задача | Статус | Комментарий |
|---|--------|--------|-------------|
| 0.1 | Создать структуру каталогов | ✅ Выполнено | `src/` приведена в соответствие с ADR-002 (Слои: `far`, `v8_artifacts`, `base`) |
| 0.2 | Подготовить демо-артефакты | ✅ Выполнено | Тестовые данные для проверки выхода из Фазы 2 и начала Фазы 3 готовы. |
| 0.3 | Настроить FAR Manager (Windows) | ✅ Выполнено | Установлен в `C:\Program Files\Far Manager` |
| 0.4 | Настроить far2l (Linux) | ✅ Выполнено | Установлен на дополнительном ноутбуке в сети (планируется клонирование и сборка `*.so` на Фазе 2) |
| 0.5 | Анализ алгоритмов CF-контейнера | ✅ Выполнено | Документ `docs/cf_processing.md` содержит полные спецификации |
| 0.6 | Анализ байт-кода | ✅ Выполнено | Документ `docs/1c_bytecode.md` |
| 0.7 | Инициализировать среду Rust | ✅ Выполнено | Rust и cargo успешно установлены. |
| 0.8 | Настроить Git-репозиторий | ✅ Выполнено | `git init` и `git add .` выполнены успешно. |

### Отклонение структуры от плана

Плановая архитектура (согласно ADR-002) была успешно перенесена в рабочую директорию. Теперь структура состоит из 3 логических слоев:

1. `src/far/` (Взаимодействие с Far Manager)
2. `src/v8_artifacts/` (Бизнес-логика форматов 1С)
3. `src/base/` (Низкоуровневые I/O операции)

Тестовые артефакты для завершения Фазы 2 и старта Фазы 3 уже подготовлены, что опережает первоначальный план для Фазы 0.

В рабочей папке создана структура слоев согласно ADR-002. В каждом модуле созданы файлы `mod.rs` с англоязычными комментариями. Среда Rust полностью готова (`Cargo.toml` настроен, зависимости прописаны). Git-репозиторий инициализирован.

---

## Стратегия DEFLATE

Для распаковки сжатых блоков CF-контейнеров используется Rust crate `flate2` — нативная реализация DEFLATE без внешних зависимостей.

| Компонент | Решение | Примечание |
|-----------|---------|------------|
| DEFLATE-декомпрессия | `flate2` (crate) | Нативная производительность, кроссплатформенность |
| DEFLATE-компрессия | `flate2` (crate) | Для CF-writer (пересборка контейнеров) |

---

## Архитектура модулей (из SRS v2.0)

```text
src/
├── lib.rs                       -- Точка входа, экспорт Far Plugin API (C ABI)
├── far/                         -- СЛОЙ 1: Взаимодействие с Far Manager
│   ├── mod.rs                   -- Инициализация, настройки, диспетчеризация вызовов
│   ├── panels.rs                -- Логика виртуальных файловых панелей (VFS для артефактов)
│   └── ui.rs                    -- Отрисовка элементов UI (диалоги, прогресс-бары, меню)
├── v8_artifacts/                -- СЛОЙ 2: Семантика и структура артефактов 1С
│   ├── mod.rs
│   ├── cf.rs                    -- Высокоуровневая обработка контейнеров CF/EPF/ERF
│   ├── metadata.rs              -- Навигация и разбор дерева метаданных конфигурации
│   ├── onecd.rs                 -- Логика парсинга внутреннего формата файловых баз .1cd
│   └── bytecode.rs              -- Байт-код: дизассемблер и структуры (opcodes)
└── base/                        -- СЛОЙ 3: Низкоуровневые библиотечные функции (I/O, парсинг)
    ├── mod.rs
    ├── reader.rs                -- Абстрактный ридер (буферизованное чтение, работа с диском/памятью)
    ├── parser.rs                -- Низкоуровневый парсер скобочных структур 1С
    └── utils.rs                 -- Утилиты (нормализация путей, кодировки, временные файлы, deflate)
```

---

## Чеклист перед стартом Фазы 1

- [x] Репозиторий создан (подготовка директории)
- [x] Структура каталогов готова
- [x] Алгоритмы cf_reader проанализированы
- [x] docs/1c_bytecode.md создан
- [x] Тестовые файлы собраны
- [x] FAR Manager установлен (`C:\Program Files\Far Manager`)
- [x] far2l установлен на Linux-хосте
- [x] Установлен Rust (cargo, rustc)
- [x] Cargo-проект инициализирован
- [x] Git-репозиторий инициализирован

---

## Фаза 1: Инфраструктура плагина

| # | Задача | Статус | Длительность | Критерий завершения |
|---|--------|--------|--------------|---------------------|
| 1.1 | Cargo-проект + Far Plugin API экспорт (C ABI) | ✅ | 4 часа | Плагин появляется в меню F11 |
| 1.2 | Реализовать GetFindDataW — базовое сканирование | ✅ | 4 часа | Панель показывает файлы |
| 1.3 | Реализовать навигацию SetDirectoryW | ✅ | 2 часа | Вход в папки работает |
| 1.4 | Подключить DEFLATE через flate2 | ✅ | 4 часа | Сжатые блоки распаковываются |
| 1.5 | Реализовать CF-парсер на Rust | ✅ | 4 часа | Чтение структуры CF работает |
| 1.6 | Тестирование на Windows | ✅ | 2 часа | Плагин работает в FAR Manager 3 |

**Выход Фазы 1:** Плагин загружается, показывает содержимое CF-контейнеров

### Задачи Фазы 1 (детализация)

**1.1 Cargo-проект + Far Plugin API**

- [x] Создать `Cargo.toml` с `crate-type = ["cdylib"]`
- [x] Реализовать `lib.rs` с `extern "C"` экспортом: SetStartupInfoW, GetPluginInfoW, OpenW, AnalyseW
- [x] **Реализовано:** Исправлено выравнивание структур (`repr(C)`) и инициализация `StructSize`.
- [x] **Реализовано:** Статическое выделение строк через `Box::leak` для предотвращения порчи памяти.
- [x] **Реализовано:** Управление дескриптором панели (`PluginPanel` struct + `ClosePanelW`).
- [x] Зарегистрировать префикс "1c:"
- [x] Тест: плагин виден в F11 → Plugins и открывает пустую панель.

**1.2 GetFindDataW — базовое сканирование**

- [x] Реализовать структуру PluginPanelItem
- [x] **Реализовано:** Синхронизация структур с локальным SDK (`plugin.hpp`).
- [x] **Реализовано:** Исправлено выравнивание и порядок полей для x64.
- [x] Читать ImageHeader через CF-парсер (пока заглушка)
- [x] Возвращать список файлов/папок
- [x] Тест: при входе в артефакт панель не пустая (подтверждено скриншотом)

**1.3 SetDirectoryW — навигация**

- [x] Обрабатывать ".." для возврата
- [x] Обрабатывать SetDirectoryW(Dir)
- [x] Обновлять заголовок панели
- [x] Тест: плагин логирует переходы по папкам

**1.4 DEFLATE через flate2**

- [x] Подключить crate `flate2`
- [x] Реализовать `base/utils.rs` (или вынести в `base/deflate.rs`)
- [x] Тест: распаковка сжатых блоков работает

**1.5 CF-парсер на Rust**

- [x] Портировать алгоритмы FileReader/StringReader из [docs/cf_processing.md](docs/1C/cf_processing.md)
- [x] Реализовать итератор Image::rows()
- [x] Тест: чтение структуры .cf файла

---

## Чеклист перед стартом Фазы 2

- [x] Плагин успешно собирается на Rust (`cdylib`)
- [x] Far Manager видит плагин в меню F11
- [x] Базовый экспорт C ABI отлажен (отсутствуют краши при входе)
- [x] Управление памятью строк (leak) и дескрипторами панелей реализовано
- [x] Навигация по папкам (SetDirectoryW) протестирована
- [x] GetFindDataW возвращает реальный список из CF-архива (заглушка на 1.3, мост отлажен)

---

## Фаза 2: MVP — EPF/ERF (AC-001)

| # | Задача | Статус | Длительность | Критерий завершения |
|---|--------|--------|--------------|---------------------|
| 2.1 | Открытие .epf/.erf как виртуальной панели | ✅ | 4 часа | Ctrl+PgDn на .epf открывает панель |
| 2.2 | Улучшенная структура и навигация | ✅ | 6 часов | Группы (Forms, Templates) вместо UUID. Вход в группы работает. ".." внутри группы возвращает на уровень выше. |
| 2.3 | Просмотр модулей (F3) | ✅ | 4 часа | Просмотр .bsl через viewer |
| 2.4 | Редактирование модулей (F4) | ░ | 8 часов | Редактирование и сохранение |
| 2.5 | CF-writer: пересборка контейнера | ░ | 12 часов | Изменённый файл валиден |
| 2.6 | Прогресс-бар для длительных операций | ░ | 2 часа | Индикация при упаковке |
| 2.7 | Резервное копирование (.bak) | ░ | 2 часа | Автосохранение копии |
| 2.8 | Тестирование на Linux (far2l) | ░ | 4 часа | Клонирование на Linux, сборка `*.so`, проверка |
| 2.9 | Исправление запуска из меню F11 | ░ | 2 часа | Список файлов не пустой при вызове из Plugin Menu |
| 2.10 | Заголовки панелей по FR-003 | ░ | 1 час | Формат: "FAR 1C:<TYPE>:<FileName>" |
| 2.11 | Локализация (RU/EN) | ░ | 3 часа | Плагин учитывает текущий язык FAR (.lng файлы) |
| 2.12 | Диалог настроек (FR-030) | ░ | 4 часа | Реализовано меню настроек с выбором стиля распаковки |

**Выход Фазы 2:** AC-001 выполнен — редактирование обработок и отчётов, исправлены критические баги UX, добавлена локализация и настройки.

> [!IMPORTANT]
> **CF-writer (2.5) — обязательный компонент MVP.** Пересборка контейнера после изменений не входит в описанный эталонный набор алгоритмов и должна быть реализована с нуля.

---

## Фаза 3: CF/CFE (AC-002, AC-005)

| # | Задача | Длительность | Критерий завершения |
|---|--------|--------------|---------------------|
| 3.1 | Открытие .cf/.cfe — иерархия метаданных | 8 часов | Дерево объектов отображается |
| 3.2 | Навигация Metadata → Objects → Modules | 4 часа | Вход до конкретного модуля |
| 3.3 | Извлечение вложенных .epf/.erf | 4 часа | Копирование вложенных обработок |
| 3.4 | Распаковка в читаемую структуру (Saby-style) | 8 часов | Структура для Git |
| 3.5 | Сборка из файловой структуры | 8 часов | Обратная упаковка |
| 3.6 | Каскадная пересборка (EPF внутри CF) | 8 часов | AC-002 выполнен |
| 3.7 | Управление составом (добавление/удаление) | 4 часа | AC-005 выполнен |

**Выход Фазы 3:** AC-002 (каскадная сборка) + AC-005 (управление составом)

---

## Фаза 4: Защищённые модули (AC-003)

| # | Задача | Длительность | Критерий завершения |
|---|--------|--------------|---------------------|
| 4.1 | Обнаружение защищённых модулей | 2 часа | Определение типа модуля |
| 4.2 | Дизассемблер байт-кода | 16 часов | Читаемый вывод инструкций |
| 4.3 | Просмотр таблиц (Const, Var, Proc) | 4 часа | Полная информация о модуле |
| 4.4 | Редактирование байт-кода (опционально) | 16 часов | Изменённый байт-код работает |

**Выход Фазы 4:** AC-003 выполнен (просмотр обязательно, редактирование опционально)

---

## Фаза 5: 1CD (AC-003, AC-004)

| # | Задача | Длительность | Критерий завершения |
|---|--------|--------------|---------------------|
| 5.1 | Изучение структуры .1cd (на основе tool1cd) | 8 часов | Понимание формата |
| 5.2 | Открытие .1cd — список конфигураций | 8 часов | CF/CFE отображаются |
| 5.3 | Вход в конфигурацию через временный файл | 4 часа | Прозрачная навигация |
| 5.4 | Извлечение .cf/.cfe из базы (GetFiles) | 4 часа | Копирование конфигурации |
| 5.5 | Запись .cf/.cfe в базу (замещение) | 16 часов | AC-004 выполнен |

**Выход Фазы 5:** AC-003 (вложенная навигация) + AC-004 (замещение)

---

## Фаза 6: Полировка и кроссплатформенность

| # | Задача | Длительность | Критерий завершения |
|---|--------|--------------|---------------------|
| 6.1 | Lazy loading для больших файлов | 8 часов | Нет OOM для > 1 ГБ |
| 6.2 | Тест на файлах 8.3.16+ | 4 часа | Совместимость |
| 6.3 | Документация и README | 4 часа | Документация готова |

---

## Оценка трудоёмкости (итого)

| Фаза | Задачи | Часы |
|------|--------|------|
| 0 | Инфраструктура | 8 |
| 1 | Инфраструктура плагина | 20 |
| 2 | EPF/ERF (MVP) | 52 |
| 3 | CF/CFE | 36 |
| 4 | Защищённые модули | 38 |
| 5 | 1CD | 40 |
| 6 | Полировка | 16 |
| **Итого** | | **210 часов** |
